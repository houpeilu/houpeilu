第十一题：
成绩排序思路：排序的一共有三组数据，分别是姓名，学号，成绩，
而姓名对应的则是字符型，成绩是int型，学号也是int型，
把这三种数据分别处理，用三个变量来代表三个数据，
把他们各自放到结构体数组中，字符型根据首字母判断就用
if（i<'z'&&i>'a'） 学号判断用1-15，
然后直接排序后输出。大致思路就是这样，冒泡排序和结构体不怎么会应用，很多错误，
所以只写了这些。
#include <stdio.h>

void sortGrade(Student students[]){
int score；
int number；
char name [15]；


int main (void)
   printf("姓名")
   printf("学号")
   printf("成绩")
}
