#include <stdio.h>

while (n)
   int a = n%2;
   int b = n/2;
   c[i] = b
   n = b/2;
   i = i+1;
}
int main (void){
   char c[0];
   for(int i = 1;i <= c.length, i++){
   printf(c[i])
   }
}
我只知道十进制变二进制，除以二取余数，得到的余数倒序排列，目前的思路只有这么多。
距离能够完成这些题目还有很长一段距离，可能真的是学习能力的问题，不过尽力了没遗憾。
